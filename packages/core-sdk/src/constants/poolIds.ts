import { PoolIdMap } from '../types'
import { ChainId } from '../enums'

export const FERMION_POOLID: PoolIdMap = {
    [ChainId.ETHEREUM]: 8,
    [ChainId.ROPSTEN]: 8,
    [ChainId.RINKEBY]: 8,
    [ChainId.GÖRLI]: 8,
    [ChainId.KOVAN]: 8,
  }